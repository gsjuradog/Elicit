import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles(() => ({
  progressBar: {
    height: '5px',
    background: '#e9d985',
    marginTop: '20px',
  },
}));
export default useStyles;
